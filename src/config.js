const BASE_URL = 'http://localhost:8000/api/';

const config = {
    APP_TITLE       : 'TERMINAL DE OMNIBUS',
    URL_MOVIMIENTOS : BASE_URL + 'movimientos',
    URL_CHOFERES 	: BASE_URL + 'choferes',
    URL_EMPRESAS 	: BASE_URL + 'empresas',
    URL_SERVICIOS 	: BASE_URL + 'servicios',
};

export default config;